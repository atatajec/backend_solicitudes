spring:
    application:
        name: dinet
    

    r2dbc:
        url: r2dbc:postgresql://localhost:5432/dbsolicitudes
        username: postgres
        password: root

server:
    address: 0.0.0.0
    port: 8080

springdoc:
  api-docs:
    path: /api-docs
  swagger-ui:
    path: /swagger-ui.html